import Logout from "./Logout";

function Navbar() {
    // Home, Add a new Book, Book List, Book Details
    return (
        <nav>
            <span>Navbar</span>
            <Logout />
        </nav>
    );
}

export default Navbar;

